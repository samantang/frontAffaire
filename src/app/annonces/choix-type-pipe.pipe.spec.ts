import { ChoixTypePipePipe } from './choix-type-pipe.pipe';

describe('ChoixTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChoixTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
